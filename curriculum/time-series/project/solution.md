# Solution guidance

For every origin, fit all state only on its history, generate exactly the target
horizon, and retain the cutoff and errors. Aggregate across origins only after
examining horizon/regime slices. A strong submission adds seasonal naive,
interval coverage, input availability assertions, and operational fallback.
