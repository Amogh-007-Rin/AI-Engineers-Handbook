# Solution guidance

Validate vector dimensions before arithmetic and test results against hand
calculations. For finite differences, compare several step sizes: large steps
have truncation error while tiny steps lose precision. A strong submission
derives the quadratic gradient, plots or tabulates relative error, states the
chosen tolerance, and connects matrix operations to features, parameters, and
batch shapes rather than treating them as detached formulas.
