# Solution guidance

Frame the decision and metric first. Split by deployment unit, fit preprocessing
inside training folds, and compare constant plus simple baselines. Report
uncertainty, calibration, slices, largest errors, and seed sensitivity. Persist
preprocessing, schema, estimator, thresholds, and environment together; justify
deployment or rejection against stakeholder costs.
