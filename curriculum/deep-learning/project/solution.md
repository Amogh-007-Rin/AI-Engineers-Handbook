# Solution guidance

Derive gradients, compare finite differences and autodiff within tolerance, and
track loss, updates, shapes, dtypes, seeds, and non-finite values. Add a validation
split, baseline, best-state restoration, and checkpoint round trip. Framework
parity means equivalent contracts and metrics, not identical floating-point bits.
