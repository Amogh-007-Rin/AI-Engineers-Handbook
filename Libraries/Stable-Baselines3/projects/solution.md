# Solution notes

The contract rejects incompatible algorithms and single-seed claims. A complete
experiment persists normalization/replay state with the model and reports return
confidence intervals, sample efficiency, and robustness.
