# Stable-Baselines3 experiment contract

Validate algorithm/action-space compatibility, seed count, evaluation budget,
and frozen normalization without training. Run `python -W error -m unittest -v`;
extend with env checker, baseline training, artifact reload, and shifted tests.
