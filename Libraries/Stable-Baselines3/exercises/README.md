# Stable-Baselines3 exercises

Run the environment checker and random baseline, train across five seeds,
freeze evaluation normalization, compare an algorithm alternative, save/load
all state, and evaluate under a held-out dynamics shift.
