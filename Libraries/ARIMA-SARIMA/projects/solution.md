# ARIMA/SARIMA solution guidance

The reference refits at every origin using only the prefix, evaluates the same targets for naive and model forecasts, and keeps the metric transparent. Its fixed drift specification is an educational fixture, not an automatic recommendation. A complete submission selects orders within each training regime or freezes them from an earlier development window and evaluates interval calibration as well as point error.
