# PyCaret auditable experiment project

Run `python3 -m unittest -v test_experiment.py` in the Python 3.11 environment. Replace the fixture with open data; document every `setup` inference, compare dummy/linear/nonlinear candidates under identical folds, tune one model, calibrate and select a threshold outside test data, inspect errors/slices, and save/reload the pipeline. Reimplement the selected workflow explicitly in Scikit-Learn and explain every difference. Passing requires 80/100 and no hidden split, feature, metric, or preprocessing decision.
