# PyCaret exercises

Configure an explicit session, compare models on a fixed split, inspect all
preprocessing steps, calibrate the selected estimator, and save/load it in a
clean process. Record every automated decision and excluded feature.
