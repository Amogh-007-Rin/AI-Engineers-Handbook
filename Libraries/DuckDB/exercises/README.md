# DuckDB exercises

Query partitioned data with explicit schemas, compare projection/filter pushdown
plans, handle duplicate and null keys, and write an idempotent incremental
feature table. Include a query plan and memory/latency measurements.
