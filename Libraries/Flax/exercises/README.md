# Flax exercises

Add a masked batch and variable collection, run `jit` with static configuration,
checkpoint and restore optimizer state, and compare two seeds with finite
gradient and shape assertions.
