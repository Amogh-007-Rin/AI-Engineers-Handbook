# Gensim sparse-text project

Fit a dictionary and TF-IDF model only on training documents, verify that
unknown validation tokens cannot mutate vocabulary state, and test both
artifacts after reload. Run `python -W error -m unittest -v`.
