# Transformers exercises

Build a local tokenizer and tiny model, test masks/truncation and label mapping,
fine-tune on a deduplicated split, report length/class slices, then save and
reload completely offline with bounded inference inputs.
