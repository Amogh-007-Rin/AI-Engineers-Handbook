# Albumentations exercises

Add crop, color, mask, and keypoint transforms; test coordinate boundaries and
empty targets; replay a failure; and measure class/box survival over 1,000
samples. Remove any transform unsupported by domain evidence.
