# Optuna exercises

Define a seeded objective with a held-out set, prune on an intermediate metric,
resume a study, and compare optimization results with a fixed baseline. Record
sampler, pruner, budget, and failed trials.
