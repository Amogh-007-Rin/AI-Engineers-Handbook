# MLflow run-evidence project

Validate a dependency-free run manifest and model-promotion gate before wiring
it to a tracking server. Run `python -W error -m unittest -v`; extend it with
real MLflow logging, registry lineage, signature validation, and rollback.
