# PySpark pipeline contract

Validate schemas, join null policy, idempotent writes, and driver-collection
safety without requiring a Spark cluster. Run `python -W error -m unittest -v`;
extend with a local DataFrame plan and representative cluster benchmark.
