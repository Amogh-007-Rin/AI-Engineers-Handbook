# OpenCV exercises

Add grayscale/alpha policies, test resize geometry on boxes and masks, compare
interpolation methods, decode untrusted bytes with dimension limits, and record
codec/version provenance for deterministic artifacts.
