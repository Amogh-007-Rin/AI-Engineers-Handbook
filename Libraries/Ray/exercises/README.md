# Ray exercises

Implement a remote map with bounded resources, an actor with checkpoint/restart,
retry a transient failure, cancel queued work, and write an idempotent sink.
Measure object-store memory and tail latency before scaling.
