# BentoML exercises

Implement typed endpoints with batching and timeout limits, package a model
bundle, add readiness/metrics, load-test back pressure, and run a canary with a
schema-compatible rollback.
