# Docker exercises

Create a multi-stage CPU inference image, run it as a numeric UID, add a real
readiness probe and graceful SIGTERM path, generate an SBOM, and compare cold
start/size against a pinned alternative.
