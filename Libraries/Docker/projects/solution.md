# Solution notes

The validator treats image identity, user, health, and resources as deployment
contracts. A production submission attaches build logs, image digest, SBOM,
scan findings, runtime metrics, and a tested rollback image.
