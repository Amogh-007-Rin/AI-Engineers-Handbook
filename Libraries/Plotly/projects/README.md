# Plotly figure contract

Build a trace with explicit hover metadata, inspect its structured spec, and
export HTML. Run `python -W error -m unittest -v`; extend it with static image,
null handling, and a browser accessibility check.
