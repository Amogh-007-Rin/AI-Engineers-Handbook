# Solution notes

Testing the trace specification catches mismatched labels and values before a
browser renders misleading output. Production exports should pin resources and
record the input query, ordering, and Plotly version.
