# Keras exercises

Implement a portable custom layer with `keras.ops`, test train/eval behavior,
restore the best validation checkpoint, and reload the native artifact under a
second backend where supported. Document backend-specific boundaries.
