# FastAPI exercises

Add request IDs and structured logs, separate liveness from readiness, enforce
timeouts around a failing dependency, and test concurrent duplicate
idempotency keys. Keep domain logic HTTP-independent and document secret
rotation and rollback behavior.
