# W&B tracking contract

Validate secret-redacted configs and bounded sweep definitions without requiring
network access. Run `python -W error -m unittest -v`; extend it with offline
artifact logging, fixed split metrics, and a report containing failed runs.
