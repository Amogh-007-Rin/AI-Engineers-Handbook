# PettingZoo parallel contract

Validate action and transition dictionaries against the active-agent set. Run
`python -W error -m unittest -v`; extend with a custom ParallelEnv, conversion
to AEC, official API tests, seeded traces, and changing-agent lifecycles.
