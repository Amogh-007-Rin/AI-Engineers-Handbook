# Statsmodels exercises

Fit a regression and diagnostic model, inspect residual assumptions, compare
robust standard errors, and run a time-aware forecast with interval coverage.
Document every interpretation boundary.
