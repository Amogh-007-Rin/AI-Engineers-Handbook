# Prophet exercises

Use rolling-origin evaluation, add a declared holiday/regressor, compare naive
and seasonal baselines, and inspect interval coverage during a regime change.
Persist the exact cutoff and feature calendar.
