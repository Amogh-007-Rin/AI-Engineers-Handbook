# Diffusers scheduler contract

Exercise a DDPM scheduler with synthetic tensors and no model download. Run
`python -W error -m unittest -v`; extend with a pinned local tiny pipeline,
generation manifest, prompt suite, safety checks, and latency/memory profiling.
