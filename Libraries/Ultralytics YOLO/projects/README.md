# YOLO dataset-contract project

Validate class IDs and normalized boxes without downloading weights. Run
`python -W error -m unittest -v`; extend with image/label identity, group-aware
splits, tiny training, per-class/size errors, and source/export parity.
