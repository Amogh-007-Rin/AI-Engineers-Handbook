# Ultralytics YOLO exercises

Audit a tiny licensed dataset, enforce group-aware splits, train a baseline,
analyze per-class/size and NMS errors, export to ONNX, compare predictions, and
benchmark latency/memory with rollback criteria.
