# Dash callback contract

Build a small layout, isolate numeric business logic, and verify callback
registration without a browser. Run `python -W error -m unittest -v`; extend
with browser accessibility, session isolation, caching, and concurrency tests.
