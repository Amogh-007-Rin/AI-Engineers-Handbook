# Dash exercises

Add a Plotly chart, validated filters, loading/error states, server-side cache,
and per-session state. Test callback cycles, concurrent users, keyboard access,
and p95 latency under representative payloads.
