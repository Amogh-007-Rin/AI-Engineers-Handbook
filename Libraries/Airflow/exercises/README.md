# Airflow exercises

Create a scheduled feature DAG with pure task functions, interval-partitioned
outputs, atomic publishing, bounded retries/timeouts, and freshness alerts.
Backfill twice and prove output idempotence.
