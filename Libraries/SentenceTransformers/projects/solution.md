# Solution notes

The project rejects mismatched or non-finite embedding spaces and makes
normalization explicit. Production retrieval must also pin the encoder revision,
evaluate real hard negatives, and rebuild or migrate the index atomically.
