# SentenceTransformers similarity contract

Normalize embeddings and compute cosine rankings without downloading a model.
Run `python -W error -m unittest -v`; extend with a pinned local model, held-out
queries, hard negatives, retrieval metrics, cache identity, and index migration.
