# Gymnasium environment contract project

Run the deterministic line world, verify reset/step signatures and spaces, and
distinguish success termination from horizon truncation. Run `python -W error -m
unittest -v`; extend it with a wrapper and an agent trace test.
