# Gymnasium exercises

Add a time-limit wrapper and prove truncation differs from termination. Record
seeded trajectories, validate every sampled action, and compare a random policy
with a deterministic baseline without leaking future rewards.
