# TorchServe exercises

Build a handler that validates JSON and tensor inputs, package a versioned
archive, secure management routes, measure batching/p99 latency, and roll back
after an injected handler regression.
