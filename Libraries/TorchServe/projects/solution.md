# Solution notes

The manifest rejects mutable versions and public management access before
deployment. Production evidence includes archive hash, handler tests, request
schema, worker/device profile, telemetry, and previous-version rollback.
