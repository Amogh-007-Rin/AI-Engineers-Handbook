# TorchServe archive contract

Validate archive identity, handler, runtime, and signature without starting a
server. Run `python -W error -m unittest -v`; extend with a real mar archive,
authenticated management API, batch load test, and rollback smoke test.
