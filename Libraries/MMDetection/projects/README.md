# MMDetection resolved-config contract

Validate required config sections, dataset/head class alignment, and immutable
weight identity without installing the compiled stack. Run `python -W error -m
unittest -v`; extend with config resolution, dataset visualization, tiny
training, evaluator output, and deployment parity.
