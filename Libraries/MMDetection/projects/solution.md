# Solution notes

The gate catches a common transfer-learning error: a detector head whose class
count disagrees with dataset metadata. Production evidence includes the printed
resolved config, exact OpenMMLab matrix, dataset audit, and export comparison.
