# MMDetection exercises

Resolve and diff an inherited config, register a small licensed dataset, audit
geometry/classes, fine-tune a pinned checkpoint, analyze AP/errors, and export
through the supported stack with source/runtime parity and performance tests.
