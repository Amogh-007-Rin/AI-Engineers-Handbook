# LightGBM exercises

Compare histogram parameters and early stopping on a time/group split, inspect
feature importance stability, calibrate probabilities, and test missing-value
and unseen-category behavior against a baseline.
