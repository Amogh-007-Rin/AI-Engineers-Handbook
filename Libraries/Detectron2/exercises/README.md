# Detectron2 exercises

Register a small licensed dataset, validate box/mask/category mappings, fine-tune
a pinned model-zoo config, report AP by class/size and error type, then export
and compare predictions, latency, memory, and unsupported operators.
