# Solution notes

The validator makes coordinate mode and category remapping explicit before
framework ingestion. A production solution also verifies image files, mask
geometry, duplicate scenes, licensed weights, checkpoints, and evaluator output.
