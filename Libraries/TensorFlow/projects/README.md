# TensorFlow contract project

Train a transparent regressor, validate every gradient, and export a typed
SavedModel signature. Under the reference environment run `python -W error -m
unittest -v`. Extend it with held-out data, a baseline, and subprocess reload.
