# NLTK tokenizer contract project

Build an offline-safe tokenizer and train-only vocabulary. Run `python -W error
-m unittest -v`. Then compare against a resource-backed tokenizer and document
which apostrophe, Unicode, URL, and number cases change.
