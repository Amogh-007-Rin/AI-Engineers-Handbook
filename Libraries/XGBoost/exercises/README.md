# XGBoost exercises

Use early stopping on a group-aware split, compare monotonic constraints,
calibrate probabilities, inspect SHAP/feature stability, and persist model plus
feature schema with unseen-category tests.
