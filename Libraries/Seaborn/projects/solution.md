# Seaborn project solution guidance

The reference uses tidy columns, explicit ordered categories, axes-level functions, raw observations layered over a distribution summary, units in labels, and semantic tests. It avoids an automatic inferential interval because the tiny fixture does not support one. A repeated-measures extension should resample subjects—not rows—and disclose the estimator, interval method, and sample size.
