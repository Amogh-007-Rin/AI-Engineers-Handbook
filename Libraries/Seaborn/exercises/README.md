# Seaborn exercises

Visualize distributions, uncertainty, and subgroup gaps with an accessible
theme. Validate aggregation denominators, avoid truncated axes, and export a
deterministic figure with its data query and version manifest.
