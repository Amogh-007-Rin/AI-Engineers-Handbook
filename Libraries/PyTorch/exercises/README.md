# PyTorch exercises

Add a variable-length batch, checkpoint optimizer and scheduler state, run a
mixed-precision comparison, and test inference mode, finite gradients, and
dynamic batch shapes against a fixed baseline.
