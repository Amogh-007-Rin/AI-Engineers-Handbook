# Dask exercises

Compare partition sizes, inspect graph fusion, add a deterministic dataframe
groupby, and write partitioned output idempotently. Record memory and scheduler
latency before claiming a speedup.
