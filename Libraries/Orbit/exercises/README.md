# Orbit exercises

Compare a local-level and seasonal model on a rolling-origin split, inspect
posterior/forecast intervals, handle missing timestamps, and serialize the
chosen estimator with its calendar and scaling assumptions.
