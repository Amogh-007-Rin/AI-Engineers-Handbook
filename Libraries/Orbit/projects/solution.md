# Orbit solution guidance

The dependency-free reference validates unique contiguous daily timestamps and produces expanding origins whose test windows never overlap their training prefix. The real smoke test verifies Orbit’s DLT fit/predict contract. Full work must compare estimators and priors, inspect diagnostics, evaluate intervals and structural change, and keep future regressors honest.
