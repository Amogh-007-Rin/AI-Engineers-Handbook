# TensorFlow Serving exercises

Deploy two signature-compatible versions, send valid and malformed REST/gRPC
requests, measure dynamic batching and p99 latency, then canary and roll back on
a quality or error-rate regression.
