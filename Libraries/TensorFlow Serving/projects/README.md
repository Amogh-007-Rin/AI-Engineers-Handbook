# TensorFlow Serving configuration contract

Validate pinned model versions and paths without starting a server. Run
`python -W error -m unittest -v`; extend with signature requests, batching
latency, readiness, and a rollback smoke test.
