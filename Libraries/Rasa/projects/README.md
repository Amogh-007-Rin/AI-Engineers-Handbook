# Rasa conversation contract

Validate fallback coverage and safe side-effecting actions without training a
model. Run `python -W error -m unittest -v`; extend with NLU data, stories,
rules, held-out conversations, action-server mocks, and handoff tests.
