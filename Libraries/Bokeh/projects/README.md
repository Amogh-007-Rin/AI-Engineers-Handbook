# Bokeh visualization contract

Create a labeled `ColumnDataSource` chart, validate inputs, and export a
standalone HTML artifact. Run `python -W error -m unittest -v`; extend it with
accessible color, uncertainty bands, and a provenance panel.
