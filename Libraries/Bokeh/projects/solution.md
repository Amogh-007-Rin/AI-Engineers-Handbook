# Solution notes

The chart keeps data columns explicit and uses a fixed title and labels. A
production artifact should embed or pin resources, include input/version hashes,
and test keyboard access and color contrast in a browser.
