# PyTorch Geometric exercises

Batch heterogeneous graph sizes, implement a convolution on a hand-computed
fixture, add global pooling, sample neighborhoods reproducibly, and compare with
a feature-only baseline under an entity/time-aware split.
