# PyTorch Geometric batching contract

Build two graphs, validate edge bounds, batch them, and inspect index offsets and
assignment vectors. Run `python -W error -m unittest -v`; extend with a message
passing layer, graph pooling, sampler, and leakage-safe evaluation.
