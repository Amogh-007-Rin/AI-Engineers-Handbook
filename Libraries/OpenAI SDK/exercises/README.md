# OpenAI SDK exercises

Mock streaming and tool responses, enforce JSON schema, add retry/backoff and
timeouts, redact traces, and build a prompt-injection/tool-authorization suite.
Run a tiny live test only with an explicit cost budget and no personal data.
