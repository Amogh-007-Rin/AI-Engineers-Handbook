# Solution notes

The validator forces evaluation to be distinct from exploratory training and
requires explicit resources/checkpoints. Production evidence also includes
return distributions, environment-step accounting, restore, and failure traces.
