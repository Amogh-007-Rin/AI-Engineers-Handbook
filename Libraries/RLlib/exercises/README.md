# RLlib exercises

Train a local PPO baseline, add fixed-seed evaluation with confidence intervals,
scale runners while comparing distributions, restore a checkpoint in a clean
process, and inject a runner failure.
