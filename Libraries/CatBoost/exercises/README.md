# CatBoost exercises

Compare native categorical handling with leakage-prone target encoding, run a
group-aware split, inspect per-slice calibration, and persist the model plus
feature schema. Explain ordered boosting and unseen-category behavior.
