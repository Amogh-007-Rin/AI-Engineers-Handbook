# Solution notes

`local_var` prevents the exercise from mutating caller-owned graph features.
The expected output exposes edge direction and feature alignment. Production
work must persist ID maps and split/sampling provenance with model state.
