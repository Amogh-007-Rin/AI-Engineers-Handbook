# DGL message-passing contract

Construct a three-node directed graph and verify incoming aggregation by hand.
Run `python -W error -m unittest -v`; extend with batched graphs, neighbor
sampling, a leakage-safe split, and state/topology round-trip.
