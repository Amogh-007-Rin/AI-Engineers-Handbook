# DGL exercises

Implement mean and attention aggregation on a hand-computable graph, batch two
graphs, sample neighborhoods under a fixed seed, and prove a time/entity split
prevents future-neighbor leakage. Profile memory and throughput.
