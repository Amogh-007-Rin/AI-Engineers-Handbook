# JAX exercises

Vectorize a scalar function with `vmap`, differentiate it with `grad`, compile
with dynamic batch sizes, and prove random keys are split rather than reused.
Record device and dtype assumptions.
