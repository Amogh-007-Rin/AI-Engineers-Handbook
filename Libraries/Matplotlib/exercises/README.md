# Matplotlib exercises

Build a publication-quality learning curve and uncertainty plot with labeled
units, accessible colors, deterministic limits, and a saved vector artifact.
Test that plots render headlessly without changing global style.
