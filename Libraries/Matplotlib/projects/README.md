# Matplotlib accessible report project

Run `python3 -m unittest -v test_report.py`. Extend the report to compare two groups, show uncertainty and missing periods honestly, and export PNG plus SVG. Add semantic tests for labels, scales, artist counts, legend text, and data limits; include alt text and a grayscale review. Benchmark a large series and justify aggregation or rasterization without hiding rare events. Passing requires 80/100 across analytical integrity, implementation, accessibility, testing, and communication.
