# Pandas exercises

Normalize a messy event table with explicit dtypes, timezone-aware timestamps,
duplicate policy, and null semantics. Build a leakage-safe grouped feature and
test idempotence across reordered input.
