# DeepFace verification governance project

Test threshold boundary behavior and require a complete biometric governance
policy without downloading face models. Run `python -W error -m unittest -v`;
extend only with consented data, representative calibration, spoof tests, and
documented human review.
