# ONNX exercises

Export a dynamic-batch model, inspect opset and graph inputs, compare two
providers where available, and reject malformed dtype/shape requests. Package
preprocessing, labels, thresholds, hashes, and runtime versions.
