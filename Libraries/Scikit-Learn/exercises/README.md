# Scikit-learn exercises

Build a leakage-safe `Pipeline` with cross-validation, compare a baseline and
calibrated model, inspect slice metrics, and persist the complete preprocessing
plus estimator contract.
